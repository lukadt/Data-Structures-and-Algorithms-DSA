-------------------------------------------------------------------------------------------------------------------------------------------------

/* Euclid's algorithm to compute greatest common denominator */
algorithm Gcd(m, n)
	Pre:	m and n are integers
	Post:	the greatest common denominator of the two integers is calculated
	if n = 0 
		return m // the value of m is the gcd.
	end if
	return Gcd(n, m) // a recursive call to the Gcd method swapping the parameters around
end Gcd

-------------------------------------------------------------------------------------------------------------------------------------------------

algorithm ToBinary(value <int>)
	Pre:		Converts a base 10 integer to base 2 (binary)
				list is a list of ints
	Post:		value has been converted to base 2
	Returns:	base 2 represenation of int
	while value > 0
		list.Add(value % 2)
		value /= 2
	end while
	return list
end ToBaseTwo